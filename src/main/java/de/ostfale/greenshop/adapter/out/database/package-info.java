/**
 * The orders in a database, reached with plain SQL through {@code JdbcClient}. The tables are
 * the adapter's business: the domain knows nothing of them, and the ports say nothing about
 * rows.
 */
package de.ostfale.greenshop.adapter.out.database;
